let prize = prompt("Set a value between 0 and 10");

prizeNum = prize.indexOf();

let message = "My selection: ";

switch (prizeNum) {
    case 0:

        message = message + "Tom"
        break;
    case 2:
        message += "Jerry"
        break;

    case 4:


        break;
        x
}

console.log(message)